services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: exam_solver_backend
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: slm_v2_app-network